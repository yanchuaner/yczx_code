# YCZX Code 协作约定

## 项目定位

YCZX Code 是一个轻量级终端 Coding Agent。产品体验参考 Claude Code，但不以复刻完整产品为目标。当前预览版只处理单个本地代码库，优先建立可理解、可测试、可替换的 Agent 基础结构。

## 当前边界

- 正式主线采用单 Agent、单 ReAct 循环。
- 首个预览版只提供只读工具：列目录、读文件、搜索文本和读取项目规则。
- 文件路径必须限制在用户指定的工作目录内。
- `.env`、密钥、凭据、私有证书和其他敏感文件默认禁止读取。
- 文件写入、任意 Shell、MCP、多 Agent、插件市场和复杂 TUI 不属于首个预览版。
- Plan-and-Solve 与 Reflection 可以在独立学习分支验证，不能绕过统一接口直接进入正式主线。

## 开发环境

- 使用 Python 3.12 和 uv，虚拟环境固定为仓库内 `.venv`。
- 使用 `uv sync --dev` 同步环境，不使用全局 `pip` 安装项目依赖。
- 新依赖使用 `uv add <package>`；开发依赖使用 `uv add --dev <package>`。
- 依赖清单和 `uv.lock` 必须保持一致。

## 代码结构

- 产品代码放在 `src/yczx_code/`，测试放在 `tests/`。
- CLI 只负责输入输出；Agent 循环、模型适配、工具系统、上下文和权限检查分别维护。
- 共用能力先通过小接口连接。没有第二个真实实现时，不提前增加复杂抽象。
- 工具返回结构化结果，不直接向终端打印；展示由 CLI 或事件渲染层负责。
- 新增代码标识符使用英文，文档和必要注释使用中文。

## 协作方式

- 不直接向 `main` 推送，使用短分支和 Pull Request；禁止强制推送或改写共享历史。
- 分支必须使用 `feat/`、`fix/`、`docs/`、`test/`、`refactor/`、`chore/`、`ci/` 或 `revert/` 前缀，主题使用小写 kebab-case。
- 一个 PR 只解决一个明确问题，并说明目的、改动、验证命令和已知限制。
- PR 标题采用 Conventional Commits；每个 PR 由另外两位成员批准，并通过全部必需检查后使用 Squash Merge。
- ReAct、Plan-and-Solve、Reflection 的学习原型由各负责人维护；涉及公共消息、工具或模型接口时先讨论接口，再修改公共模块。
- 不覆盖其他成员未提交的改动，不代替他人创建、合并或推送 PR。

## 完成标准

提交 PR 前至少运行：

```bash
uv run ruff check .
uv run pytest
git diff --check
```

涉及 CLI 时，再手动运行 `uv run yczx --help`。无法完成的检查必须在 PR 中说明原因。

## 安全规则

- 不提交真实 API Key、`.env`、数据库、日志、上传文件或个人资料。
- 不打印完整密钥、完整提示词或可能包含敏感内容的工具输出。
- 不读取工作目录外的路径，不跟随会逃逸工作目录的符号链接。
- 不用模型输出直接拼接文件路径或命令；所有工具参数必须先校验。
- 扩展写入或命令执行能力前，必须单独设计权限、确认、审计、超时和恢复机制。
